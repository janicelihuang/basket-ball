{
  "name": "basket-ball",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://janicelihuang.github.io/basket-ball",
  "proxy": "http://localhost:8080/",
  "dependencies": {
    "csv-parser": "^2.1.0",
    "jquery": "^3.3.1",
    "pg": "^7.6.1",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-modal": "^3.6.1",
    "react-scripts": "2.1.1",
    "react-table": "^6.8.6"
  },
  "scripts": {
    "client": "npm run start",
    "server": "node src/server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "concurrently": "^4.1.0",
    "gh-pages": "^2.0.1"
  }
}
